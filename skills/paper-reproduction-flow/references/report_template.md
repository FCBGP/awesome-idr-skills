# 论文复现报告模板 (Reproduction Report)

> 由"可视化报告官(A8 · REPORTER)"填写,经"评审质控官(A9 · CRITIC)"放行后交付。
> 复制本模板到本次复现的 `RUN_WORKSPACE/results/report.md` 填写(`RUN_WORKSPACE` 即
> `create_run_workspace.py` 创建的 `YYYYMMDD-论文核心机制` 目录)。

## 1. 论文信息
- 标题:
- 作者 / 会议年份:
- 类型(勾选): 仿真系统安全 / 实证数据 / ML数值 / 理论
- 投入规模: 完整版(10) / 标准版(6) / 精简版(3)

## 2. 复现目标(来自 repro_spec)
- Claim 1:
- Claim 2:
- (每张图/表/关键结论一条)

## 3. 复现结果逐项对齐
| 复现单元 | 论文预期 | 本复现实测 | 偏差(delta) | 结论(pass/fail/partial) |
|---------|---------|-----------|------------|------------------------|
|         |         |           |            |                        |

## 4. 偏差项归因(来自 diagnoses/tickets)
- 单元 X 偏差根因: [读错目标/假设方法/数据不一致/实现bug/配置采样不足]
- 已采取的回溯与修复:

## 5. 假设台账摘要(assumption_ledger)
| 假设 | 依据 | 影响 | 置信 |
|------|------|------|------|
|      |      |      |      |

> 所有偏离论文的假设/配置必须在此显式列出,接受质控复核。

## 6. 环境说明(env_lock)
- 运行时 / 版本:
- 数据集与版本(如 CAIDA Jan-2016 as-rel):
- 随机种子:
- 依赖锁定:

## 7. 结论
- 复现度总体判定: [完全复现 / 部分复现 / 未能复现 / 论文本身不可复现]
- 本复现是否支持原论文结论:
- 重要限制与 caveats:

## 8. 质控意见(review_notes)
- 是否存在为凑数字反向调参(过拟合):
- 比较口径是否公平一致:
- 放行 / 打回:
